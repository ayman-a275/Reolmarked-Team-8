<UserControl x:Class="Reolmarked.View.MonthlySettlementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reolmarked.View"
             xmlns:helper="clr-namespace:Reolmarked.Helper"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#f8f9fa">

    <Grid Margin="20">
        <Border Style="{StaticResource CardStyle}" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                    <TextBlock Text="Udestående Afregninger" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="#212529"
                               VerticalAlignment="Center"/>
                    <Border Background="#007bff" 
                            CornerRadius="5" 
                            Padding="8,4" 
                            Margin="15,0,0,0">
                        <TextBlock Text="{Binding SettlementSummaries.Count}" 
                                   Foreground="White" 
                                   FontWeight="Bold"
                                   FontSize="14"/>
                    </Border>
                </StackPanel>

                <ListView Grid.Row="1" 
                         ItemsSource="{Binding SettlementSummaries}"
                         Background="White"
                         BorderBrush="#dee2e6"
                         BorderThickness="1"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         AlternationCount="2">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15"
                                    Margin="5"
                                    CornerRadius="6"
                                    BorderThickness="2">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding NetAmount, Converter={x:Static helper:NegativeToTrueConverter.Instance}}" Value="True">
                                                <Setter Property="Background" Value="#f8d7da"/>
                                                <Setter Property="BorderBrush" Value="#dc3545"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding NetAmount, Converter={x:Static helper:PositiveToTrueConverter.Instance}}" Value="True">
                                                <Setter Property="Background" Value="#d4edda"/>
                                                <Setter Property="BorderBrush" Value="#28a745"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,8">
                                        <TextBlock Text="{Binding RenterName}" 
                                                   FontWeight="Bold" 
                                                   FontSize="18" 
                                                   Foreground="#212529"/>
                                        <TextBlock FontSize="14" 
                                                   Foreground="#6c757d"
                                                   Margin="0,2,0,0">
                                            <Run Text="Kontonr: "/>
                                            <Run Text="{Binding RenterAccountNumber}" FontWeight="SemiBold"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <TextBlock Grid.Row="0" Grid.Column="1" 
                                               Text="{Binding NetAmount, StringFormat='{}{0:C}', ConverterCulture=da-DK}" 
                                               FontWeight="Bold" 
                                               FontSize="24" 
                                               VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding NetAmount, Converter={x:Static helper:NegativeToTrueConverter.Instance}}" Value="True">
                                                        <Setter Property="Foreground" Value="#dc3545"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding NetAmount, Converter={x:Static helper:PositiveToTrueConverter.Instance}}" Value="True">
                                                        <Setter Property="Foreground" Value="#28a745"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                               Orientation="Horizontal" 
                                               Margin="0,8,0,8">
                                        <TextBlock Text="Leje: " FontWeight="SemiBold" Foreground="#495057"/>
                                        <TextBlock Text="{Binding TotalRent, StringFormat='{}{0:C}', ConverterCulture=da-DK}" 
                                                   Foreground="#212529" Margin="0,0,20,0"/>

                                        <TextBlock Text="Salg: " FontWeight="SemiBold" Foreground="#495057"/>
                                        <TextBlock Text="{Binding TotalSales, StringFormat='{}{0:C}', ConverterCulture=da-DK}" 
                                                   Foreground="#212529" Margin="0,0,20,0"/>

                                        <TextBlock Text="Kommission: " FontWeight="SemiBold" Foreground="#495057"/>
                                        <TextBlock Text="{Binding Commission, StringFormat='{}{0:C}', ConverterCulture=da-DK}" 
                                                   Foreground="#212529"/>
                                    </StackPanel>

                                    <Button Grid.Row="2" Grid.Column="1" 
                                           Content="Marker som betalt" 
                                           Command="{Binding DataContext.MarkAsPaidCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                           CommandParameter="{Binding}"
                                           Style="{StaticResource SuccessButton}"
                                           Padding="15,8"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>