<UserControl x:Class="Reolmarked.View.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reolmarked.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="220"
             Background="#f8f9fa">

    <Border Background="White" 
            CornerRadius="12,0,0,12" 
            Padding="0" 
            Margin="0">
        <Border.Effect>
            <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="3" BlurRadius="10" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    Background="#007bff" 
                    CornerRadius="0,12,0,0" 
                    Padding="20,9">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Menu" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            x:Name="CloseMenuBtn" 
                            Style="{StaticResource CloseButton}">
                        <Image Source="/Data/img/close_menu.png" Height="25" Width="25"/>
                    </Button>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="12,20">
                <StackPanel>
                    <Button x:Name="ProductBtn" Content="Vare" Style="{StaticResource MenuButton}"/>
                    <Button x:Name="ShelfBtn" Content="Reol" Style="{StaticResource MenuButton}"/>
                    <Button x:Name="RenterBtn" Content="Lejer" Style="{StaticResource MenuButton}"/>
                    <Button x:Name="RentShelfBtn" Content="Leje af reol" Style="{StaticResource MenuButton}"/>

                    <Separator Margin="8,20" Background="#dee2e6"/>

                    <Button x:Name="PaymentBtn" Content="Betaling" Style="{StaticResource PrimaryMenuButton}"/>
                    <Button x:Name="MonthlySettlementBtn" Content="Månedlig Afregning" Style="{StaticResource InfoMenuButton}"/>
                </StackPanel>
            </ScrollViewer>

            <Separator Grid.Row="2" Margin="8,0" Background="#dee2e6"/>

            <StackPanel Grid.Row="3" Margin="12,15">
                <Button x:Name="ExitBtn" Content="Luk Program" Style="{StaticResource DangerMenuButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>