<UserControl x:Class="Reolmarked.View.MenuClosedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reolmarked.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="60"
             Background="#f8f9fa">

    <Border Background="White" 
            CornerRadius="0,12,12,0" 
            Padding="0" 
            Margin="0"
            Width="60">
        <Border.Effect>
            <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="3" BlurRadius="10" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    Background="#007bff" 
                    CornerRadius="0,12,0,0" 
                    Padding="5">
                <Button x:Name="OpenMenuBtn" 
                        Style="{StaticResource OpenMenuButton}"
                        ToolTip="Åbn menu" Margin="-3,0,-2,0"
                        Background="#007bff">
                    <Image Source="/Data/img/open_menu.png" Height="25" Width="25"/>
                </Button>
            </Border>
        </Grid>
    </Border>
</UserControl>